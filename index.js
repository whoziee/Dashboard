// const io = require("socket.io-client");

// // Connect to the WebSocket using the API endpoint
// const socket = io("https://data.gdscnsut.com");

// // Listen for the 'random_number' event and log the received number
// socket.on("random_number", (data) => {
//     console.log("Received random number:", data);
// });

// socket.on("connect", () => {
//     console.log("Connected to WebSocket!");
// });

// socket.on("connect_error", (error) => {
//     console.error("Connection failed:", error);
// });
